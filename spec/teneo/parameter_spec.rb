# frozen_string_literal: true

RSpec.describe Teneo::Parameter do
  it "has a version number" do
    expect(Teneo::Parameter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
